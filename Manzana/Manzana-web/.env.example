# Copy to .env and fill with your Supabase project credentials
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: restrict admin access by email(s) and/or domain
# Comma-separated list of allowed admin emails
VITE_ADMIN_EMAILS=
# Single allowed domain, e.g. example.com (users with this email domain are allowed)
VITE_ADMIN_DOMAIN=
